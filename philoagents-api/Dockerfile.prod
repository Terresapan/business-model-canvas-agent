#
# --- THIS IS YOUR PRODUCTION DOCKERFILE ---
# --- Save as: philoagents-api/Dockerfile.prod ---
#

FROM python:3.11-slim

# Install uv.
COPY --from=ghcr.io/astral-sh/uv:latest /uv /uvx /bin/

# Set the working directory.
WORKDIR /app

# 1. Copy the file that defines your dependencies.
COPY pyproject.toml README.md ./

# 2. Create the virtual environment.
RUN uv venv

# 3. Install the project and all its dependencies from pyproject.toml.
# This command correctly installs "extras" (like [standard]).
RUN uv pip install .

# 4. Copy the application source code into the container.
COPY src/philoagents philoagents/

# 5. This is the production-ready command.
# It runs the fast 'uvicorn' server without the slow '--reload' flag.
# Cloud Run will automatically pass in the correct --port (8080).
CMD ["/app/.venv/bin/fastapi", "run", "philoagents/infrastructure/api.py", "--host", "0.0.0.0"]